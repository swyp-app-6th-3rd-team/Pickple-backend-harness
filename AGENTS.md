# Pickple Backend 작업 규칙

## 적용 원칙

- 사용자 요청과 실제 저장소 상태를 우선한다. 문서나 추측이 코드·설정과 다르면 현재 상태를 확인하고 차이를 알린다.
- 작업 전 Git 루트, 현재 브랜치, 원격, 변경 상태를 확인한다. 기존 변경은 사용자의 작업으로 보고 보존한다.
- 로컬 파일 변경은 허용된 작업 범위 안에서만 한다. 커밋, 푸시, PR 생성·수정, 배포는 사용자가 명시적으로 요청한 경우에만 수행한다.
- 코드 작업을 마치면 작업한 브랜치와 실행한 검증, 실행하지 못한 검증을 함께 알린다.
- 단순 오타나 명백한 소규모 수정에는 무거운 절차를 강제하지 않는다.

## 프로젝트 기준

- Java 25, Spring Boot 4.1, Gradle Wrapper를 사용한다.
- 기본 패키지는 `app.pickple` 이다(도메인 `pickple.app` 의 역순 DNS). 새 클래스는 이 아래에 둔다.
- 데이터베이스 변경은 Flyway 마이그레이션으로 관리하고, JPA 스키마 자동 변경에 의존하지 않는다.
- QueryDSL은 현재 `build.gradle`에 선언된 OpenFeign 포크와 APT 설정을 유지한다.
- 통합 테스트는 MySQL Testcontainers를 사용하므로 로컬 Docker 사용 가능 여부를 먼저 구분한다.
- 인증 코드와 비밀값을 다룰 때 실제 토큰, 키, 비밀번호를 코드·로그·문서·응답에 복사하지 않는다.

## Git 규약

- 브랜치 이름은 `타입/#이슈번호-작업명` 형식을 지킨다. 예: `feature/#12-profile-update`
- 타입은 `feature` `feat` `fix` `hotfix` `refactor` `chore` `docs` `test` `perf` `build` `ci` 중 하나다.
- 작업을 시작하기 전에 대상 이슈 번호를 확인한다. 이슈가 없으면 먼저 이슈를 만든다.
- 셸에서 `#`은 주석이 될 수 있다. 브랜치 이름은 따옴표로 감싼다.
  ```
  git switch -c "feature/#12-profile-update"
  ```
- 이슈 번호는 프로젝트 보드의 `In Progress` 전이에 쓰인다. 번호가 없으면 보드가 움직이지 않는다.
- `.githooks/pre-push`가 이 형식을 검사한다. `develop`과 `main`은 검사하지 않는다.
- 훅은 `core.hooksPath`로 연결되며 Gradle 빌드가 한 번 돌면 자동 설정된다. 수동 설정은 `git config core.hooksPath .githooks`.
- 브랜치 이름 규약을 벗어나야 하면 `git push --no-verify`로 우회하고 PR에 사유를 적는다.
- `develop`은 rebase로 당기지 않는다. `git pull`(merge)만 쓴다.
- `.githooks/pre-rebase`가 `git pull --rebase`와 `pull.rebase=true`를 막는다. 내 브랜치의 `git rebase -i`는 막지 않는다.
- `git pull --rebase --no-verify`는 `pre-rebase`를 우회하지 못한다. 다만 `git rebase --no-verify`는 우회한다. 훅은 실수 방지지 강제가 아니다.
- `pull.rebase`가 켜져 있으면 끈다. 전역 설정도 `git config --global --get pull.rebase`로 확인한다.
- develop PR은 `Merge pull request`로만 병합한다. `Rebase and merge`는 저장소 설정에서 꺼져 있다.

## 작업 흐름

1. 요청을 관찰 가능한 결과 한 문장으로 고정하고, 포함 범위와 제외 범위를 확인한다.
2. 관련 코드, 설정, 테스트, 문서를 좁게 탐색해 현재 계약과 관례를 확인한다.
3. 원인 불명 버그, 성능 문제, 모호한 요구, 여러 컴포넌트가 얽힌 문제에는 `resolve-problem` 스킬을 사용한다.
4. 세 단계 이상이거나 의존 순서가 있는 작업은 짧은 계획을 세우고 진행 상태를 갱신한다.
5. 가장 작은 검토 가능한 수직 단위로 구현하고, 범위를 벗어난 변경은 섞지 않는다.
6. 변경 범위에 맞춰 코드 리뷰, 테스트, 보안 관점으로 점검한다.
7. 원래 사용자 시나리오와 회귀 위험을 검증한 뒤 결과를 보고한다.

자동 에이전트 전환, 특정 도구 호출, PRD·ADR·PR 생성은 모든 작업의 필수 단계가 아니다. 필요한 경우에만 사용한다.

## 사고 규율

비자명한 문제는 다음 원칙을 따른다.

- 탐색 전에 가장 가능성 높은 가설과 그것을 반증할 증거를 적는다.
- 사실, 추론, 미확인 가정을 구분한다.
- 표면 증상에서 멈추지 말고 수정 가능한 원인까지 인과관계를 좁힌다.
- 광범위한 전체 탐색보다 심볼, 호출 경로, 실패 로그, 변경 이력을 목적 있게 확인한다.
- 성능 판단은 추측이 아니라 호출 횟수, 지연시간, 쿼리 계획, CPU·메모리 같은 측정값으로 한다.
- 가장 불확실하면서 값싸게 검증 가능한 가정을 먼저 실험한다. 임시 spike는 제품 코드에 남기지 않는다.
- 도구 출력은 그대로 나열하지 않고, 무엇을 의미하며 다음 판단에 어떻게 연결되는지 설명한다.
- 외부 시스템이 근본 원인이고 더 진행할 근거가 없으면 불확실성과 확인 방법을 명시하고 멈춘다.

## 설계와 기술 도입 게이트

API 계약, DB 스키마, 주요 패키지 경계, 외부 시스템·미들웨어, 주 언어·런타임처럼 되돌리기 비싼 결정에만 적용한다.

- 현재 한계를 형용사가 아닌 수치 또는 재현 가능한 현상으로 정의한다.
- 기존 스택 개선, 자료구조·알고리즘 변경, 새 기술을 포함한 현실적인 대안을 비교한다.
- 아무것도 바꾸지 않을 때의 비용과 위험도 대안 비용에 포함한다.
- 구현 비용뿐 아니라 운영 부담, 장애 모드, 결합도, 마이그레이션·철회 비용을 함께 계산한다.
- 가장 불확실한 가정은 작은 spike나 PoC로 검증하고 성공·실패 기준을 먼저 정한다.
- 성능 개선을 주장할 때는 같은 조건의 변경 전·후 절대값과 측정 방법을 함께 남긴다.
- API 결정은 endpoint·method·요청/응답 DTO·오류·하위 호환 계약을, DB 결정은 스키마·Flyway·인덱스 근거와 기존 데이터 영향을 먼저 정리한다.
- 영향받는 패키지·모듈, 선행 조건, 아직 닫히지 않은 질문을 구현 전에 드러낸다.
- 측정이 새 기술을 가리키면 채택할 수 있지만, 미래 대비만을 이유로 복잡성을 추가하지 않는다.
- 결정이 장기적으로 남고 되돌리기 비싸면 `docs/adr/`에 맥락, 결정, 결과, 검토한 대안과 기각 사유를 기록한다.

## Java/Spring 구현 규칙

- 새 제품 기능은 가능한 한 기능 단위로 `domain`, `service`, `infra`, `controller` 경계를 분명히 한다.
- 도메인 객체는 Spring, JPA, Bean Validation, Lombok에 의존하지 않으며 상태 전이와 불변식을 스스로 지킨다.
- 저장소 포트인 `*Store` 인터페이스는 도메인에 두고, JPA 구현과 `*Entity`, Spring Data Repository는 `infra`에 둔다.
- 서비스와 컨트롤러가 Spring Data Repository 또는 JPA 엔티티에 직접 의존하지 않게 한다.
- 컨트롤러는 요청 검증과 DTO 변환을 담당하고 JPA 엔티티나 Spring Data `Page`·`Window`를 API 응답으로 직접 노출하지 않는다.
- 필드 주입 대신 생성자 주입을 사용한다.
- 시간 의존 로직은 주입된 `Clock`을 사용하고, 저장 정밀도와 비교 정밀도를 맞춘다.
- 새 마이그레이션은 순방향 파일로 추가한다. 이미 적용됐을 가능성이 있는 마이그레이션 변경은 영향과 복구 방법을 먼저 확인한다.

## API 문서 (Scalar·Swagger UI·llms.txt)

세 문서 표면이 같은 OpenAPI 스펙 하나를 읽는다. 아래 셋은 **새 API 를 추가하거나 응답 모양을 바꿀 때마다** 함께 한다 — 나중에 몰아서 채우면 초기 도메인처럼 60% 가 비어버린다.

- **인증이 필요한 엔드포인트에는 `@SecurityRequirement(name = "bearerAuth")` 를 반드시 붙인다.**
  springdoc 은 `SecurityConfig` 를 읽지 않으므로 이 애노테이션이 없으면 문서에 자물쇠가 안 뜨고,
  FE 는 토큰이 필요한지 알 수 없다. `SecurityConfig` 의 `permitAll`·`PUBLIC_GET` 에 있는
  공개 엔드포인트에는 **붙이지 않는다**(값 없는 `@SecurityRequirements` 로 푸는 방식도 쓰지 않는다 — 근거는 ADR-0034).
  새 인증 API 를 추가하면 `SecurityConfig` 와 이 애노테이션을 **함께** 손댄다.
  **ArchUnit 이 이것을 강제한다** — `ArchitectureTest.ApiDocumentation` 이 permitAll 이 아닌
  핸들러에 애노테이션이 없으면 실패한다. 공개 엔드포인트 목록은 그 테스트의
  `PUBLIC_ENDPOINTS` 에도 있으니 `SecurityConfig` 와 함께 고친다.

- **컨트롤러에는 `@Tag` 를 붙이고 이름은 도메인 명사 단수·파스칼로 통일한다.**
  `Auth`·`User`·`Post`·`Comment`·`Vote`·`Grade`·`Badge`·`Ranking`·`Image` 가 현재 목록이다.
  빠뜨리면 springdoc 이 `vote-controller` 같은 클래스명 파생 태그를 만들어 Scalar 사이드바 표기가 어긋난다.
  `description` 은 그 도메인이 무엇을 다루는지 한 줄로 적는다.

- **응답·요청 DTO 의 모든 필드에 `@Schema(description = ...)` 를 붙인다.**
  하우스 스타일은 한국어 명사구, 마침표 없음. null 조건은 뒤에 한 문장으로(`"투표 인원. 일반 게시글은 null"`).
  enum 은 값을 나열하고(`"GENERAL | AGREE | A_B"`), 규칙 ID 는 본문에 인라인으로 넣는다(`"...여야 한다(R-10)"`).
  `example` 은 짧은 스칼라에만 쓴다. javadoc `@param` 이 이미 있으면 그것을 압축해 쓰고 둘을 함께 둔다.
  **필드 의미를 지어내지 않는다** — 코드·SPEC·runbook 에서 확인되지 않으면 최소 서술로 두고 PR 에 남긴다.

## 패키지·폴더·파일 네이밍

- 새 패키지나 파일을 만들기 전에 같은 기능과 같은 역할의 기존 파일을 먼저 확인한다. `src/main/java`와 대응하는 `src/test/java`에서 가장 가까운 패키지 구조, 폴더 구조, 파일명과 접미사를 우선해 따른다.
- 제품 코드는 기능 우선 구조인 `app.pickple.<기능>.<계층>`을 기본으로 한다. 기존 기능명인 `auth`, `comment`, `item`, `point`, `post`, `vote`를 재사용하고, 역할에 따라 `domain`, `service`, `infra`, `controller` 중 적절한 위치에 둔다.
- `domain`에는 도메인 모델과 저장 포트인 `*Store`, `service`에는 유스케이스를 조합하는 `*Service`, `infra`에는 `Jpa*Store`, `*Entity`, `*Repository`와 외부 연동 구현, `controller`에는 HTTP 진입점과 요청·응답 타입을 둔다.
- 애플리케이션 부트스트랩 설정과 설정값 타입은 루트 `app.pickple.config`에 두고 `<기능>.config` 하위 패키지를 만들지 않는다. 특정 기능에 밀접한 설정 클래스는 `AppleTokenClientConfiguration`, `DocsConfig`처럼 해당 기능 패키지에 둘 수 있다.
- 기능 도메인에 속하지 않는 애플리케이션 전역 코드와 기술 경계만 `common`, 루트 `config`, `docs`, `error`에 둔다. 특정 기능이 소유하는 코드를 편의상 공통 패키지에 두지 않는다.
- 기존 구조에 없는 `dto`, `repository`, `entity` 등의 중간 패키지를 일반적인 습관만으로 새로 만들지 않는다. 다른 구조가 필요하면 생성 전에 이유와 영향 범위를 밝힌다.
- Java 패키지 선언은 디렉터리 경로와 일치시킨다. 파일명은 최상위 타입명과 동일한 PascalCase로 작성하고, 같은 역할에는 기존 접미사를 재사용한다.
- 특정 운영 클래스를 검증하는 테스트는 운영 코드와 같은 상대 패키지에 둔다. 예를 들어 `comment/controller/CommentController.java`의 테스트는 `comment/controller`에 둔다.
- 단위·경량 테스트는 `대상명Test`, Spring Context·DB·Testcontainers·LocalStack·전체 HTTP 경계를 포함하는 통합 테스트는 `대상명IT`로 이름 짓는다.
- 통합 테스트 클래스명은 `*IT`로 끝낸다. 통합 테스트 환경을 구성하는 공용 애노테이션 이름은 `IntegrationTest`로 유지한다.
- 이 규칙을 추가했다는 이유만으로 관련 없는 기존 파일을 일괄 이동하거나 이름 변경하지 않는다.

## 리뷰·QA·보안 체크리스트

코드 변경 시 관련 항목만 점검한다.

- 정확성: 요구사항, 기존 API 계약, 경계값, null·빈 입력, 오류 경로, 동시성 영향
- 구조: 명확한 이름, 중복, 계층 역방향 의존, 불필요한 추상화, 변경 범위 이탈
- 데이터: N+1, 불필요한 로딩, 트랜잭션 경계, 인덱스·정합성, 마이그레이션 안전성
- 테스트: happy path와 주요 edge case, 실패 경로, given/when/then 가독성, 고정 대기나 환경 의존성
- 보안: 인증·인가 우회, IDOR, injection, SSRF, 역직렬화, 토큰·쿠키 처리, 과도한 권한
- 보안 설정: 암호화·서명 검증, 기본 자격 증명·안전하지 않은 기본값, 취약 의존성, 민감정보 로깅·감사 누락
- 비밀값: 발견 사실은 파일과 위치만 알리고 실제 값은 출력하지 않는다.
- 발견 사항은 가능하면 심각도, 재현 조건, 영향, 수정 방향을 함께 제시한다.

## 검증

- 변경에 가장 가까운 테스트부터 실행하고 필요할 때 범위를 넓힌다.
- Gradle 명령은 운영체제에 맞는 Wrapper를 사용한다. Windows는 `gradlew.bat`, macOS/Linux는 `./gradlew`를 사용한다.
- Java 코드 변경에는 최소한 컴파일과 관련 테스트를 확인한다.
- Flyway 변경에는 파일명·버전, fresh DB 적용, JPA 검증 영향을 확인한다.
- Testcontainers가 Docker 부재로 실행되지 않으면 코드 실패와 환경 차단을 구분하고, 통과한 범위와 미검증 범위를 모두 적는다.
- 빌드 성공만으로 사용자 시나리오가 충족됐다고 단정하지 않는다.

## 문서와 GitHub

- 여러 작업 단위에 걸친 기능은 필요할 때 `docs/prd/`에 무엇을 왜 만드는지, 포함·제외 범위, 측정 가능한 완료 조건, 열린 질문을 기록한다.
- 단일 수정이나 내부 리팩터링에는 PRD·ADR 작성을 강제하지 않는다.
- PR 작업을 요청받은 경우 `.github/pull_request_template.md` 형식을 사용하고 실제로 확인한 검증만 표시한다.
- GitHub Actions 결과를 확인하지 않았다면 통과했다고 쓰지 않는다.
- 기존 ADR 파일은 당시 결정의 기록이므로 수정하거나 삭제하지 않는다.
- 기존 결정을 변경하거나 보완할 때는 `docs/adr/`에서 가장 큰 번호의 다음 4자리 번호와 영문 kebab-case를 사용해 `NNNN-<decision-name>.md`를 새로 추가한다.
- 새 ADR에는 대체·보완하는 기존 ADR과 적용 범위를 명시하고, 맥락, 결정, 결과(트레이드오프), 검토한 대안과 기각 사유를 기록한다.
- 대체 관계와 상태는 새 ADR 및 `docs/adr/README.md` 인덱스에 반영하며, 기존 ADR 본문에는 추가하지 않는다.
- ADR은 기존 설계 게이트에 해당하는 장기적이고 되돌리기 비싼 결정에만 작성한다.
